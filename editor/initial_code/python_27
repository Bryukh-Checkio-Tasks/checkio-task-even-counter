#Your optional code here
#You can import some modules or create additional functions


def checkio(line):
    #Your code here
    #It's main function. Don't remove this function
    #It's using for auto-testing and must return a result for check.  

    #replace this for solution
    return line

#Some hints
#You can convert string to integer with int()
#You can convert int to string with str()
#Don't forget -- string is like a list.
#Maybe regexp

#These "asserts" using only for self-checking and not necessary for auto-testing    
if __name__ == '__main__':
    assert checkio(123456) == 3, '1st example'
    assert checkio(111111) == 0, '2nd example'
    assert checkio(2468) == 4, '3rd example'
